# AGENTS.md

## Project Overview

Minimal Rust workspace with a single crate. No tests, no CI, no README.

## Build & Run

```bash
cargo build
cargo run
```

## Notes

- Uses Rust edition 2024 (requires recent toolchain)
- No test suite exists
- No formatting/linting config present